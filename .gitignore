fileCrypto/json/*
!fileCrypto/json/filepath.json
fileCrypto/cipher/*
!fileCrypto/cipher/cipherText.md
fileCrypto/plain/*
!fileCrypto/plain/plainText.md
fileCrypto/decode/*
!fileCrypto/decode/decodeText.md
fileCrypto/key/*
!fileCrypto/key/AES-CTR.key

config/key.json

# 以下は github で ignore ファイルを go 言語で作ったら default で生成されたもの
# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/
